import styled from 'styled-components';

export const InputWrapper = styled.div`
  + .wcl-dropdown__heading__label {
    display: none;
  }

  input {
    color: inherit;
    background: transparent;
    border: none;
    outline: none;
    padding: 0;
  }
`;
