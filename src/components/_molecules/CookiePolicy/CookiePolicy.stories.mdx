import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import Button from '../../_atoms/Button';
import Icon from '../../_atoms/Icon';
import Text from '../../_atoms/Text';
import { action } from '@storybook/addon-actions';
import CookiePolicy from './CookiePolicy.component.tsx';

<Meta title="Core|Molecules/CookiePolicy" />

## CookiePolicy Component

#### Default

<Preview>
  <Story name="default">
    <CookiePolicy
      text={<Text.Body>We use cookies to ensure that we give you the best experience on our website. <Text.Link href="#" target="_blank">Read our privacy policy</Text.Link> and let us know that you agree to cookies.</Text.Body>}
      acceptButtonContent={'AGREE TO COOKIES'}
      userAction={action('cookie-click')}
    />
  </Story>
</Preview>

# Props

<Props of={CookiePolicy} />
