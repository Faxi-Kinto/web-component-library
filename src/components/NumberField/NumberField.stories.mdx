import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { withKnobs, text, boolean } from '@storybook/addon-knobs';
import NumberField from './';

export const listArray = [
  { value: '00381', label: '+381' },
  { value: '00434', label: '+434' },
  { value: '00321', label: '+321' },
  { value: '0044', label: '+44' },
  { value: '001', label: '+1' },
];

<Meta title="Core|Molecules/NumberField" />

# NumberField Component

Number field with dropdown list (of country calling codes). It implements [FormField API](/docs/core-molecules-formfield--default-story).

It is intended to be used inside `Form` and it returns Object `{country:'', phone:''}`

## Implementations

#### Default

<Preview>
  <Story name="default" decorators={[withKnobs]}>
    <NumberField
      list={listArray}
      onChange={data => console.log(data)}
      placeholder={text('Placeholder text', '')}
      description={text('Description text', '')}
      error={text('Error message', 'This field is required')}
      touched={boolean('Touched', false)}
      dirty={boolean('Dirty', false)}
    />
  </Story>
</Preview>

#### With error state

<Preview>
  <Story name="With error state">
    <NumberField
      list={listArray}
      error={'Some error string'}
      touched={true}
      dirty={true}
    />
  </Story>
</Preview>

## Props

<Props of={NumberField} />
