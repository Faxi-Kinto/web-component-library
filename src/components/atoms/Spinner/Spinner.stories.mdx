import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import Spinner from './';

<Meta title="Core|Atoms/Spinner" />

# Spinner Component

Displays a circular spinner, accepts props that describe the color of the spinner and the background the spinner will be on (which is transparent by default)

## Implementation

#### Default

<Preview>
  <Story name="default">
    <Spinner color="#00708D" />
  </Story>
</Preview>

## Props

<Props of={Spinner} />
